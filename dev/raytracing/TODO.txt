- When user is using the "Alt-click & rotate" to move about an object, it is very violent when clicking on something in the background. Fix: limit the distance at which this feature is activated.
- When sphere is really small and user is in sphere, it just blocks entire view. Fix: drop sphere if really small.

