#! /usr/bin/env python

import os, sys

os.chdir('/build')
for repo in ['FXrays', 'cypari', 'plink', 'snappy_manifolds', 'spherogram', 'snappy']:
    os.system('git clone https://github.com/3-manifolds/' + repo + '.git')
