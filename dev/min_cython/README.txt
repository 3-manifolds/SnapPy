==================
Cython test module
==================

A small Cython module showing how to share Cython classes across
several "pyx" files or even separate modules. 

To test in place (builds binary module cymodule_one.so and
cymodule_two.so), do::

  python test.py
